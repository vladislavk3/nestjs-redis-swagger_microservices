import { GameConfig } from './game-config.util';

xdescribe('Game Util', () => {
  beforeAll(async () => {});

  xdescribe('Test GameConfig Class', () => {
    it('GameConfig should be defined', () => {
      expect(GameConfig).toBeDefined();
    });
  });

  afterAll(async () => {});
});
